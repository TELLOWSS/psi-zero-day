name: G2 Runner Sentinel

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/g2-runner-sentinel.yml'
      - 'docs/defense/G2_RUNNER_SENTINEL.md'
  workflow_dispatch:

jobs:
  sentinel:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Runner allocated
        run: |
          echo "G2_RUNNER_SENTINEL=PASS"
          uname -a

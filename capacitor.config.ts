import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tellowss.psizeroday',
  appName: 'PSI : ZERO DAY',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
